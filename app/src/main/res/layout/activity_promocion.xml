<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/etTituloPromo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Título de la promoción" />

    <EditText
        android:id="@+id/etDescripcionPromo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Descripción" />

    <EditText
        android:id="@+id/etFechaPromo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Fecha" />

    <Button
        android:id="@+id/btnGuardarPromocion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Guardar promoción" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerPromociones"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"/>

</LinearLayout>
